import React from "react";
import { BrowserRouter, Route, Routes } from "react-router-dom";

function Router() {
  return (
    <BrowserRouter>
      <Route></Route>
    </BrowserRouter>
  );
}

export default Router;
